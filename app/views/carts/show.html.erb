<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<h2> The cart show page </h2>
<ul>    
  <% @cart.line_items.each do |item| %>
  <li><%=  item.quantity %> & items; <%= item.product.title %></li>
    <!-- <li><%= item.product.title %></li> -->
  <% end %>
</ul>


<%= link_to 'Edit', edit_cart_path(@cart) %> |
<%= link_to 'Back', carts_path %>
